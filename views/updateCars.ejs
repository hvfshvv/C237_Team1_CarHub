<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>CarHub App</title>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/inventory">Car Hub</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
              <a class="nav-link" href="/addCar">Add Car</a>
            </li> 
            <li class="nav-item">
              <a class="nav-link" href="/inventory">View Cars</a>
            </li> 
            
            <li class="nav-item">
              <a class="nav-link" href="/viewusers">View Users</a>
            </li> 
            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li> 
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main content -->
  <div class="container mt-4">
    <h2 class="text-center mb-4">Update car</h2>
    
    <% if (car) { %>
      <form action="/updateCars/<%= car.carId %>" method="POST" enctype="multipart/form-data" class="mx-auto" style="max-width: 500px;">
        
        <div class="mb-3">
          <label for="carModel" class="form-label">Car Model</label>
          <input type="text" class="form-control" id="carModel" name="carModel" value="<%= car.carModel %>" required>
        </div>

        <div class="mb-3">
          <label for="Year" class="form-label">Year Manufactured</label>
          <input type="text" class="form-control" id="Year" name="Year" value="<%= car["Year"] %>" required>
        </div>

    
        <div class="mb-3">
          <label for="price" class="form-label">Price (SGD)</label>
          <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" value="<%= car.price %>" required>
        </div>

        <div class="mb-3">
          <label for="description" class="form-label">Description</label>
          <input type="text" class="form-control" id="description" name="description" value="<%= car.description %>" required>
        </div>

        <div class="mb-3">
         <label for="status" class="form-label">Status (Available / Sold)</label>
         <input type="text" class="form-control" id="status" name="status" value="<%= car.status %>" required>
        </div>

        <div class="mb-3">
          <label class="form-label">Current Image</label>
          <input type="text" class="form-control" name="currentImage" value="<%= car.image %>" readonly>
          <img src="/images/<%= car.image %>" class="img-thumbnail mt-2" style="width: 150px;">
        </div>

        <div class="mb-4">
          <label for="image" class="form-label">Upload New Image</label>
          <input class="form-control" type="file" id="image" name="image" accept="image/*">
        </div>

        <div class="d-grid gap-2">
          <button type="submit" class="btn btn-primary">Update car</button>
          <a href="/inventory" class="btn btn-secondary">Back to Inventory</a>
        </div>
      </form>
    <% } else { %>
      <p class="text-center">No car found.</p>
    <% } %>
  </div>
</body>
</html>
